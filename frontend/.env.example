# Environment Variables for ROASTME

# Public API URL (leave as /api for Next.js API routes)
NEXT_PUBLIC_API_URL=/api

# WebSocket URL (for separate WebSocket server if deployed separately)
# Leave empty if using Socket.IO in serverless mode or alternative solution
NEXT_PUBLIC_WS_URL=

# Optional: External services
# Add any API keys or external service URLs here
